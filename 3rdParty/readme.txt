

There are 2 scripts that will help fetch and build the dependencies needed.

  fetch.bat       - grab library archives from the web, extract them
  build_x64.bat   - build the things that need to be built

